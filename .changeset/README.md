This directory stores Changesets files.

Run `pnpm changeset` after making a user-facing change to a publishable package in
`packages/*`, then commit the generated markdown file in this directory with your code
changes.
